# StaticWebPage 发布规则

新增、更新、替换或发布静态页面及 Ref 文件时，必须使用仓库级 `$publish-static-pages` Skill：

```text
.agents/skills/publish-static-pages/SKILL.md
```

该 `SKILL.md` 是本仓库发布规则的唯一导航入口。必须从入口按任务读取其指定文档并完整执行相应流程；不得绕过入口使用旧流程。

全程使用中文与用户沟通。
