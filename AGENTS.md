# StaticWebPage 发布规则

当用户要求添加新的静态页面时：

1. 检查 HTML 中是否存在缺失的本地图片、样式或脚本资源。
2. 如果用户要求替换首页，则更新根目录 `index.html`。
3. 其他页面使用小写连字符 slug，放入 `<slug>/index.html`。
4. 保留页面原始内容和视觉效果，不做未经要求的设计修改。
5. 在提交前进行本地 HTTP 访问检查，并验证页面内的本地资源引用。
6. 提交到 `main` 分支并推送，让 Vercel 自动部署。
7. 不提交密钥、令牌、Cookie、`.env` 或其他凭据。
